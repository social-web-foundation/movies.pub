apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "movies-pub.fullname" . }}-test-connection"
  labels:
    {{- include "movies-pub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "movies-pub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
