apiVersion: v1
kind: Service
metadata:
  name: {{ include "movies-pub.fullname" . }}
  labels:
    {{- include "movies-pub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "movies-pub.selectorLabels" . | nindent 4 }}
